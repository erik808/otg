function [A1] = randomgraph(size)
  A0  = zeros(size);
  A1  = rand(size);
  rng = (A1 > 0.999);
  A0(rng) = (A1(rng));
  A1  = sparse(A0);
  A1  = triu(A1) + triu(A1)';
  A1  = A1 - diag(diag(A1));

end
